name: AML Triage CI/CD Pipeline

on:
  push:
    branches: [ "master" ]

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-


      - name: Cache DVC outputs
        uses: actions/cache@v4
        with:
          path: |
             .dvc/cache
          key: ${{ runner.os }}-dvc-${{ hashFiles('dvc.lock') }}
          restore-keys: |
            ${{ runner.os }}-dvc-


      - name: Install dependencies
        run: | 
          pip install -r requirements.txt
          pip install -r requirements-test.txt
     

      - name: Run Pipeline
        env:
          DAGSHUB_TOKEN: ${{ secrets.AML_TEST }}
          DAGSHUB_USERNAME: ${{ secrets.DAGSHUB_USERNAME }}
        run: |
          dvc remote modify myremote auth basic
          dvc remote modify myremote user virajdeshmukh080818
          dvc remote modify myremote password $DAGSHUB_TOKEN
          dvc pull
          dvc repro
          dvc push

    
      - name: Run Model Evaluation
        env:
          DAGSHUB_TOKEN: ${{ secrets.AML_TEST }}
          DAGSHUB_USERNAME: ${{ secrets.DAGSHUB_USERNAME }}
        run: python src/model/model_evaluation.py

     
      - name: Run Model Registry
        env:
          DAGSHUB_TOKEN: ${{ secrets.AML_TEST }}
          DAGSHUB_USERNAME: ${{ secrets.DAGSHUB_USERNAME }}
        run: python src/model/model_registry.py


      - name: Run Model Tests
        env:
          DAGSHUB_TOKEN: ${{ secrets.AML_TEST }}
          DAGSHUB_USERNAME: ${{ secrets.DAGSHUB_USERNAME }}
        run: python -m pytest tests/test_model.py

      
      - name: Promote Model to Production
        if: success()
        env:
          DAGSHUB_TOKEN: ${{ secrets.AML_TEST }}
          DAGSHUB_USERNAME: ${{ secrets.DAGSHUB_USERNAME}}
        run:
          python scripts/promote_model.py

        
      - name: Run FastAPI App tests
        if: success()
        env:
          DAGSHUB_TOKEN: ${{ secrets.AML_TEST }}
          DAGSHUB_USERNAME: ${{ secrets.DAGSHUB_USERNAME}}
        run: python -m pytest tests/test_app.py


      # ================= DEPLOYMENT STEPS =================
      # - name: Configure AWS Credentials
      #   if: success()
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: ${{ secrets.AWS_REGION }}

      # - name: Login to AWS ECR
      #   if: success()
      #   id: login-ecr
      #   uses: aws-actions/amazon-ecr-login@v2

      # - name: Build, Tag, and Push Docker Image
      #   if: success()
      #   env:
      #     ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      #     ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
      #     IMAGE_TAG: ${{ github.sha }}
      #   run: |
      #     # By this point, 'dvc pull' has downloaded the pkl file into the runner.
      #     # Docker build will package it into the image!
      #     docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
      #     docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

      # - name: Set Up kubectl
      #   uses: azure/setup-kubectl@v3

      # - name: Update kubeconfig
      #   run: |
      #     aws eks update-kubeconfig --region ${{ secrets.AWS_REGION }} --name aml-copilot-cluster

      # - name: Create Kubernetes Secret
      #   run: |
      #     kubectl create secret generic aml-secrets \
      #       --from-literal=DAGSHUB_TOKEN=${{ secrets.DAGSHUB_TOKEN }} \
      #       --dry-run=client -o yaml | kubectl apply -f -

      # - name: Deploy to EKS
      #   run: |
      #     kubectl apply -f k8s/deployment.yaml
      #     kubectl apply -f k8s/service.yaml